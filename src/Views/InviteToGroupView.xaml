<UserControl x:Class="VRCGroupTools.Views.InviteToGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <StackPanel>
        <TextBlock Text="Invite To Group" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
        <TextBlock Text="Search a user or paste a userId, then send an invite." Foreground="#AAA" FontSize="12"/>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Search users..."
                     Width="260"
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     Foreground="White" CaretBrush="White"/>
            <Button Content="Search"
                    Command="{Binding SearchCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}" Height="32" Width="100" Margin="10,0,0,0"/>
        </StackPanel>

        <ItemsControl ItemsSource="{Binding SearchResults}" Margin="0,6,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="#2b2b2b" Padding="8" CornerRadius="4" Margin="0,4,0,0">
                        <DockPanel>
                            <StackPanel>
                                <TextBlock Text="{Binding DisplayName}" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding UserId}" Foreground="#aaa" FontSize="12"/>
                            </StackPanel>
                            <Button Content="Use" Command="{Binding DataContext.UseUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}" Height="28" Width="60" HorizontalAlignment="Right" Margin="10,0,0,0"/>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <TextBox Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="user_..."
                     Width="260"
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     Foreground="White" CaretBrush="White"/>
            <Button Content="Send Invite"
                    Command="{Binding SendInviteCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" Height="32" Width="120" Margin="10,0,0,0"/>
        </StackPanel>
        <TextBlock Text="{Binding Status}" Foreground="#7C4DFF" Margin="0,6,0,0"/>
    </StackPanel>
</UserControl>
